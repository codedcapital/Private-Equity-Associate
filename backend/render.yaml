{
  "buildCommand": "pip install -e .[dev]",
  "startCommand": "alembic upgrade head && uvicorn api.main:app --host 0.0.0.0 --port 10000 --workers 1",
  "pythonVersion": "3.11",
  "env": "python",
  "rootDir": "backend"
}